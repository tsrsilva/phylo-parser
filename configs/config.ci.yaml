# config.ci.yaml — used only inside CI; outputs to subdirectories under outputs/

input:
  data_dir: "data/examples"

resources:
  dicts_dir: "dicts"

output:
  csv_dir: "outputs/csv"
  json_dir: "outputs/json"
  missing_dir: "outputs/missing"
